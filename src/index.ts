import type { User } from "@/types/user";

const user1: User = {
  id: 1,
  name: "Yuzu",
  email: "Yuzu_0204@outlook.com",
};

console.log(`User ID: ${user1.id}, Name: ${user1.name}, Email: ${user1.email}`);